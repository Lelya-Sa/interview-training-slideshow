{
  "name": "interview-training-root",
  "private": true,
  "description": "Root for Vercel – app lives in slideshow-app/ (enable Include Git Submodules)",
  "scripts": {
    "install-all": "cd slideshow-app && npm install && cd server && npm install && cd ../client && npm install",
    "build": "cd slideshow-app && node build-direct.js && cd .. && node -e \"require('fs').cpSync('slideshow-app/api','api',{recursive:true})\""
  }
}
