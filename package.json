{
  "name": "interview-training-slideshow",
  "private": true,
  "description": "App at repo root for Vercel (no submodules)",
  "scripts": {
    "install-all": "npm install && cd client && npm install",
    "build": "node build-direct.js"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
