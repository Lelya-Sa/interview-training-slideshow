{
  "name": "interview-training-slideshow",
  "version": "1.0.0",
  "description": "Full-stack interview training slideshow with React frontend and Node.js backend",
  "main": "server/index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && node index.js",
    "client": "cd client && npm start",
    "build": "cd client && npm run build && cd .. && cp -r client/build build || xcopy /E /I client\\build build",
    "build:vercel": "cd client && npm run build",
    "install-all": "npm install && cd server && npm install && cd ../client && npm install",
    "start": "npm run dev"
  },
  "keywords": ["react", "nodejs", "slideshow", "training", "interview"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
