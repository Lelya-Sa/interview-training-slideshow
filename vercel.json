{
  "buildCommand": "cd client && npm install && npm run build && cd .. && rm -rf build 2>/dev/null; mv client/build build || cp -r client/build build",
  "outputDirectory": "build",
  "installCommand": "npm run install-all",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/$1"
    },
    {
      "source": "/((?!api/).*)",
      "destination": "/index.html"
    }
  ]
}
